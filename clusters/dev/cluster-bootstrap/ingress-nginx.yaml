#apiVersion: argoproj.io/v1alpha1
#kind: Application
#metadata:
#  name: nginx
#  namespace: argocd
#spec:
#  project: default
#  source:
#    repoURL: <twój-adres-repo>
#    path: charts/ingress-nginx/ingress-nginx-4.12.0
#    targetRevision: HEAD
#  destination:
#    server: https://kubernetes.default.svc
#    namespace: nginx
#  syncPolicy:
#    automated:
#      prune: true
#      selfHeal: true

spec:
  template:
    spec:
      containers:
      - name: argocd-repo-server
        volumeMounts:
        - name: repo-volume
          mountPath: /repo
      volumes:
      - name: repo-volume
        hostPath:
          path: /Users/damian.jankowski/src/playground/cluster-main
          type: Directory
